services:
 - type: web
   name: jam-board
   env: node
   buildCommand: npm install
   startCommand: npm start
   plan: free

services:
 - type: web
   name: jam-board
   env: node
   buildCommand: npm install
   startCommand: npm start
   plan: free
   envVars:
     - key: DB_FILE
       value: /data/jam-board.db
   disks:
     - name: data
       mountPath: /data
       sizeGB: 1
